apiVersion: v1
kind: Service
metadata:
  name: promptledger-api
  labels:
    app.kubernetes.io/name: promptledger
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: promptledger
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
